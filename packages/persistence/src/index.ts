/**
 * @eon/persistence — browser IndexedDB adapter package.
 *
 * Placeholder until Milestone 10 (tasks K01–K05). Persistence stores engine
 * snapshots and command logs but never decides simulation rules
 * (CLAUDE.md package responsibilities).
 */
export const PERSISTENCE_PACKAGE_STATUS = "placeholder-until-milestone-10" as const;
