/**
 * @eon/ui — React app chrome, inspector, timeline, tree, charts and tools.
 *
 * Placeholder until Milestone 7 (tasks H01–H06). React receives only
 * low-frequency telemetry, events and selected-entity details — never
 * high-frequency organism positions (CLAUDE.md React boundary).
 */
export const UI_PACKAGE_STATUS = "placeholder-until-milestone-7" as const;
