/**
 * @eon/renderer — PixiJS world renderer package.
 *
 * Placeholder until Milestone 6 (tasks G05–G10). The renderer is a projection
 * only: it never decides attacks, food allocation, deaths, reproduction,
 * mutations, species or authoritative positions (CLAUDE.md renderer boundary).
 * PixiJS 8.x is already pinned as a dependency per the bootstrap toolchain
 * policy, but no rendering code exists yet by design.
 */
export const RENDERER_PACKAGE_STATUS = "placeholder-until-milestone-6" as const;
